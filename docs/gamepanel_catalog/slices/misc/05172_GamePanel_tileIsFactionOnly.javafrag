// Extracted from src/mechanist/GamePanel.java
// Owner: GamePanel
// Method: tileIsFactionOnly
// Lines: 5172-5174
// Category: misc
// This is an examination slice, not compiled source.

    boolean tileIsFactionOnly(char ch) {
        return ch == 'p' || ch == 'b' || ch == 'h' || ch == 'n' || ch == 'M';
    }

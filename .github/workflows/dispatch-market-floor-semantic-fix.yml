name: Dispatch Market Floor Semantic Fix

on:
  pull_request:
    branches: [ main ]
    paths:
      - '.github/workflows/dispatch-market-floor-semantic-fix.yml'

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch resolver repair workflow
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl --fail-with-body \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/one-shot-market-floor-semantic-fix.yml/dispatches \
            -d '{"ref":"main"}'

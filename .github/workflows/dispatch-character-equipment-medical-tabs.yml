name: Dispatch Character Equipment And Medical Tabs

on:
  pull_request:
    branches: [ main ]
    paths:
      - '.github/workflows/dispatch-character-equipment-medical-tabs.yml'

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch verified implementation workflow
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl --fail-with-body \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/one-shot-character-equipment-medical-tabs.yml/dispatches \
            -d '{"ref":"main"}'

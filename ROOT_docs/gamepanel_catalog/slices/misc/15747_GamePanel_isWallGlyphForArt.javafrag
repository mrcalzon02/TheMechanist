// Extracted from src/mechanist/GamePanel.java
// Owner: GamePanel
// Method: isWallGlyphForArt
// Lines: 15747-15749
// Category: misc
// This is an examination slice, not compiled source.

    boolean isWallGlyphForArt(char ch) {
        return ch == '#' || ch == '%' || ch == '&' || ch == '^' || ch == '8' || ch == '0';
    }

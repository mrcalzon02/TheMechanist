// Extracted from src/mechanist/GamePanel.java
// Owner: GamePanel
// Method: isProtectedTransitionTile
// Lines: 4832-4834
// Category: misc
// This is an examination slice, not compiled source.

    boolean isProtectedTransitionTile(char ch) {
        return ch == 'D' || ch == 'S' || ch == 'v' || ch == 'E';
    }
